import { HendleMenuDirective } from './hendle-menu.directive';

describe('HendleMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new HendleMenuDirective();
    expect(directive).toBeTruthy();
  });
});
